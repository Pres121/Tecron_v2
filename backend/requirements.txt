# --- Core (required for training AND Raspberry Pi inference) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3

# --- FastAPI backend (required for app/api.py, deployed on Render) ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Web UI (required only if running app/web.py, Flask version) ---
flask>=3.0

# --- Training / analysis only (NOT required on the Raspberry Pi) ---
matplotlib>=3.7
seaborn>=0.12
pytest>=7.4
httpx>=0.27
